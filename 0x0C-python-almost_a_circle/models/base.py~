#!/usr/bin/python3
"""base class"""

class Base:
    """The Base Class"""

    __nb_objects = 0

    def __init__(self, id=None):
        """init the Base Class"""
        if id is not None:
            self.id = id
        else:
            type(self).__nb_objects += 1
            self.id = self.__nb_objects

        
